# ── Database ──
db.url=jdbc:postgresql://localhost:5433/your_database
db.user=your_user
db.password=your_password
db.driver=org.postgresql.Driver

# ── Application ──
app.name=HotelNova
app.version=1.0
view.type=swing   # console | swing | javafx

# ── Business rules ──
hotel.tax.rate=0.19
hotel.default.currency=COP

# ── Reports ──
report.dir=reports/